# Eidolon-AI mastermind group
 

# Getting setup
Install packages
```commandline
pip install -r requirements.txt
```

Run the server
```commandline
eidolon-server -m local_dev hello_world
```
Once up and running, check out the [API documentation](http://localhost:8080/docs)